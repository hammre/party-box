{
    "title": "Multiple-Choice Answer",
    "properties": {
        "question-identifier": {
            "type": "string",
            "maxLength": 30
        },
        "selection": {
            "type": "object",
            "properties": {
                "label": { "type": "string", "maxLength": 200 },
                "answer-identifier": {"type":"string", "maxLength": 30 }
            },
            "required": ["answer-identifier"]
        }
    },
    "required": ["question-identifier", "selection"]
}
